### List of Practical's: 
1. Program to print a message. 
2. Program to take name, age, and marks as input and display them. 
3. Program to check the data type of variables using type(). 
4. Program to perform all arithmetic operations on two numbers. 
5. Program to show relational operator results between two numbers. 
6. Program to test logical operators (and, or, not). 
7. Program to demonstrate compound assignment operators (+=, -=, etc.). 
8. Program using the ternary operator to find the largest of two numbers. 
9. Program to check whether a number is even or odd using if-else. 
10. Program to find the greatest of three numbers using if-elseif-else. 
11. Program to print numbers from 1 to 10 using a for-loop. 
12. Program to print numbers from 1 to 10 using a while loop. 
13. Program to print even numbers using for and range(). 
14. Program to calculate the sum of the first N natural numbers using a loop. 
15. Program using nested loops to print a pattern (e.g., a rectangle of stars). 
16. Program to demonstrate the break statement inside a loop. 
17. Program to demonstrate the continue statement inside a loop. 
18. Program using for-else (e.g., searching for an item in a list). 
19. Program using while-else (e.g., counter loop finishing normally). 
20. Program to input a string and print its length. 
21. Program to count vowels in a string. 
22. Program to check if a substring exists in a string. 
23. Program to reverse a string using slicing. 
24. Program to demonstrate string methods (upper(), lower(), replace(), etc.). 
25. Program to create a list and display all its elements. 
26. Program to find the largest and smallest element in a list. 
27. Program to count even and odd numbers in a list. B.A./B.A. (Hons)/B.A. (Hons. with research) Major & Minor Courses Third Year (Semester System), Examinations, 2026-27 
28. Program to demonstrate list methods (append, remove, sort, etc.). 
29. Program to take a list input from the user and print it using traversal. 
30. Program to demonstrate nested lists and print elements. 
31. Program to create a tuple and access its elements. 
32. Program to count occurrences of an element using count(). 
33. Program to find index of an element using index(). 
34. Program to create a dictionary of student details and print keys & values. 
35. Program to add, update, or delete items from a dictionary. 
36. Program to iterate through dictionary using for key in dict. 
37. Program to check if a key exists in a dictionary. 
38. Program to define a simple function and call it. 
39. Program to add two numbers using a user-defined function. 
40. Program to calculate the factorial of a number using a function. 
41. Program to check if a number is prime using a function. 
42. Program to demonstrate default arguments in a function. 
43. Program to demonstrate return statement. 
44. Program using a lambda function to find the square and cube. 
45. Program to pass a list to a function and print its sum. 
46. Program to create a simple class with attributes and methods. 
47. Program to define a class Student and display details of objects. 
48. Program to demonstrate constructor `(init)`. 
49. Program to create multiple objects of a class. 
50. Program to import a class from another file and use it. 
51. Program to demonstrate try and except. 
52. Program to handle `ZeroDivisionError`. 
53. Program to handle `FileNotFoundError`. 
54. Program using try-except-else block. 
55. Program to accept integer input and handle invalid inputs. 
56. Program to create and write to a text file. 
57. Program to read a text file and display its contents. 
58. Program to append new lines to an existing file.
59. Program to count the number of lines in a file. 
60. Program to use seek() to move the file pointer to a specific position. 
61. Program to copy content from one file to another. 
62. Program to write list elements to a file and read them back. 
63. Program to read a file and count words/characters.